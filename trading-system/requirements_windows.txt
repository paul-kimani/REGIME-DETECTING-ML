# ============================================================
# DEPRECATED — this file is no longer used.
#
# The architecture no longer has a separate Windows bridge.
# MetaTrader5 runs directly on Windows via core/execution/mt5_connector.py.
#
# Use requirements.txt in the project root instead:
#   pip install -r requirements.txt
# ============================================================
