# ============================================================
# Windows full system requirements
# Includes the MT5 bridge + the complete trading system.
# Install with:  pip install -r requirements_windows.txt
# ============================================================

# Core data science
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
statsmodels==0.14.2
scikit-learn==1.5.0

# ML models
xgboost==2.0.3
hmmlearn==0.3.2
optuna==3.6.1

# PyTorch  (CPU build — default)
# For GPU:  pip install torch==2.3.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torch==2.3.1

# Technical analysis (pandas-ta only — TA-Lib requires a C compiler on Windows)
pandas-ta==0.3.14b0

# MetaTrader5 bridge (Windows only)
MetaTrader5==5.0.45

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.30

# Cache
redis==5.0.6

# MLflow experiment tracking
mlflow==2.13.2

# HTTP / bridge
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
tenacity==8.3.0
slowapi==0.1.9

# Config / serialisation
pydantic==2.7.1
python-dotenv==1.0.1
pyyaml==6.0.1

# Scheduling (run_monitor.py)
schedule==1.2.2

# Charting (backtesting reports)
matplotlib==3.9.0

# Testing
pytest==8.2.2
pytest-cov==5.0.0
pytest-mock==3.14.0

# Utilities
requests==2.32.3
joblib==1.4.2
